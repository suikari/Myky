<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="teamgyodong.myky.user.mapper.UserMapper">
	<select id="getUser" parameterType="hashmap" resultType="teamgyodong.myky.user.model.User">
		SELECT *
		FROM USERS
		WHERE USER_ID = #{userId}
	</select>
	
	<update id="updatePwd" parameterType="hashmap">
		  UPDATE USERS
			SET
			PASSWORD = #{pwd}
			WHERE USER_ID = #{userId}
	</update>
	<select id="selectUser" parameterType="hashmap" resultType="teamgyodong.myky.user.model.User">
		SELECT USER_ID,USERNAME,NICKNAME,EMAIL,PROFILE_IMAGE,PHONE_NUMBER,BIRTH_DATE, ADDRESS,ROLE,GENDER
		FROM USERS
		WHERE 1=1
		<if test="userId != null">
		AND USER_ID = #{userId}
		</if>
		<if test="phoneNumber != null and userName != null">
		AND PHONE_NUMBER = #{phoneNumber}
        AND USERNAME = #{userName}
		</if>
	</select>
	
<!--		<if test="searchOption != null and searchOption =='all'">
		AND (TITLE LIKE '%' ||#{keyword}||'%'
		OR USERNAME LIKE '%' ||#{keyword}||'%')
	</if>
	<if test="searchOption != null and searchOption =='title'">
		AND TITLE LIKE '%' ||#{keyword}||'%'
	</if>-->
	
	
	<select id="selectNick" parameterType="hashmap" resultType="teamgyodong.myky.user.model.User">
		SELECT *
		FROM USERS
		WHERE NICKNAME = #{nickName}
	</select>
	
	<insert id="insertUser" parameterType="hashmap" >
		INSERT INTO USERS(USER_ID,USERNAME,PASSWORD,NICKNAME,EMAIL,PHONE_NUMBER,BIRTH_DATE,ADDRESS,CREATED_AT,ROLE,GENDER,AGREE_YN,EMAIL_YN,PHONE_YN) 
		VALUES (#{userId},#{userName},#{pwd},#{nickName},#{email},#{phoneNumber},#{birthDate},#{address},SYSDATE,'USER',#{gender},'Y',#{emailYn},#{phoneYn})   
	</insert>
	
	
</mapper>
